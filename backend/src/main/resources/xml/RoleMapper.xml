<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zj.examsystem.mapper.RoleMapper">

    <select id="selectDistinctRole" resultType="com.zj.examsystem.entity.Role">
        SELECT *
        FROM role
        GROUP BY role_name
    </select>
</mapper>
