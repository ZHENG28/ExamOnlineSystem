export default class User {
  constructor(account, password, username, roleName) {
    this.account = account
    this.password = password
    this.username = username
    this.roleName = roleName
  }
}
